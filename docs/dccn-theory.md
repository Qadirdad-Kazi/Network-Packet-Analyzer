# DCCN Theory Integration (Placeholder)

This project demonstrates key Data Communication & Computer Networks (DCCN) concepts:

- Routing algorithms (Dijkstra, min-hop, min-delay, load balancing)
- Packet transmission, propagation, and queueing delays
- TCP/UDP protocol behaviors (retransmission, ACK, loss tolerance)
- OSI layer encapsulation/decapsulation
- Real-time metrics: latency, throughput, packet loss

Replace this placeholder with a full theory integration report as development progresses.